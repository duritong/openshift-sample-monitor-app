apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: backend
  labels:
    app: backend-app
spec:
  selector:
    matchLabels:
      app: backend-app
  endpoints:
  - port: web
    path: /metrics
    interval: 10s
